<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.ziang.mudisk.mapper.RoleMapper">

    <insert id="addUserRole">
        insert into user_role values(null,#{userId},1)
    </insert>

    <insert id="addUserAdminRole">
        insert into user_role values(null,#{userId},2)
    </insert>

</mapper>